<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MediBook</title>

  <!-- Bootstrap -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >

  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      background: #020202;
      color: #1e293b;
      height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .navbar {
      background: transparent;
      padding: 24px 0;
    }

    .brand {
      font-weight: 600;
      letter-spacing: -0.4px;
      color: #0f172a;
      text-decoration: none;
      font-size: 18px;
    }

    .main-wrapper {
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
    }

    .card-landing {
      background: #ffffff;
      padding: 60px 50px;
      border-radius: 20px;
      box-shadow: 0 15px 40px rgba(15, 23, 42, 0.06);
      max-width: 520px;
      width: 100%;
      border: 1px solid rgba(15, 23, 42, 0.04);
    }

    .card-landing h1 {
      font-size: 24px;
      font-weight: 600;
      margin-bottom: 14px;
      color: #0f172a;
    }

    .card-landing p {
      font-size: 15px;
      color: #64748b;
      margin-bottom: 30px;
      line-height: 1.6;
    }

    .btn-main {
      padding: 11px 22px;
      border-radius: 12px;
      font-weight: 500;
    }

    .btn-dark {
      background-color: #1e3a5f;
      border-color: #1e3a5f;
    }

    .btn-dark:hover {
      background-color: #162d4a;
      border-color: #162d4a;
    }

    .btn-outline-dark {
      border-color: #1e3a5f;
      color: #1e3a5f;
    }

    .btn-outline-dark:hover {
      background-color: #e8eef4;
      color: #1e3a5f;
    }

    footer {
      padding: 22px;
      text-align: center;
      font-size: 13px;
      color: #94a3b8;
    }
  </style>
</head>

<body>

  <!-- NAVBAR -->
  <div class="container">
    <div class="navbar d-flex justify-content-between">
      <a href="/medibook" class="brand">MediBook</a>

      <div>
        <a href="/medibook/login" class="btn btn-outline-dark btn-sm me-2">
          Login
        </a>
        <a href="/medibook/register" class="btn btn-dark btn-sm">
          Create account
        </a>
      </div>
    </div>
  </div>

  <!-- MAIN CENTER -->
  <div class="main-wrapper">
    <div class="card-landing">

      <h1>Modern healthcare booking</h1>

      <p>
        A simple and secure platform connecting patients,
        practitioners and administrators in one unified system.
      </p>

      <div class="d-flex justify-content-center gap-3">
        <a href="/medibook/login" class="btn btn-dark btn-main">
          Get started
        </a>

        <a href="/medibook/practitioner/register" class="btn btn-outline-dark btn-main">
          Join as practitioner
        </a>
      </div>

    </div>
  </div>

  <!-- FOOTER -->
  <footer>
    © <%= new Date().getFullYear() %> MediBook. All rights reserved.
  </footer>

</body>
</html>
